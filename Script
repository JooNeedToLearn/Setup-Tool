#!/data/data/com.termux/files/usr/bin/bash

cd "$HOME" 2>/dev/null || cd
set +e

PREFIX="${PREFIX:-/data/data/com.termux/files/usr}"
GOFILE_ID="QeEs1Q"
MAIN_REPO="deb https://packages.termux.dev/apt/termux-main stable main"

clear
echo "======================================"
echo " KelNara One Command Setup"
echo "======================================"

echo "[1/8] Reset repo Termux..."
mkdir -p "$PREFIX/etc/apt/sources.list.d"
rm -f "$PREFIX